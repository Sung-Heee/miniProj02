<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.miniproj02.board.BoardMapper">

    <select id="getList" resultType="BoardVO">
        select
            b.*, m.member_nickname board_writer
        from tb_board b inner join tb_member m on b.member_id = m.member_id
        order by board_id desc limit #{skip}, #{size}
    </select>

    <select id="getTotalCount" resultType="int">
        select
            count(*)
        from tb_board
    </select>

    <select id="view" resultType="BoardVO">
        select
            b.*, m.member_nickname board_writer
        from tb_board b inner join tb_member m on b.member_id = m.member_id
        where board_id = #{board_id}
    </select>
</mapper>